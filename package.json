"dependencies": {
  "@supabase/supabase-js": "^2.0.0",  // or latest version
  "react-router-dom": "^6.0.0",     // or latest version
  "react": "^18.0.0",                // or latest version
  "react-dom": "^18.0.0",            // or latest version
  // other dependencies
}

